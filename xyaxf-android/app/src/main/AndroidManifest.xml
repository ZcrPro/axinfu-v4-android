<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zhihuianxin.xyaxf">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/${APP_ICON}"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">

        <activity
            android:name=".app.LunchActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.SplashActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".app.login.LoginActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".app.main.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:label=""
            android:name=".app.login.LoginSelectCityActivity"
            android:screenOrientation="portrait" />

        <activity
            android:label=""
            android:name=".app.login.LoginSelectSchoolActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
